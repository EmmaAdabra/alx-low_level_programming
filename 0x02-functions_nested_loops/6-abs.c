#include "main.h"

/**
 * _abs - print the absolute value of any given integer
 * @n: the inputed argument should be of type integer
 * Return: abs of the inputed value
 **/

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
